Install prerequisites:

 sudo apt-get install -y \
  gcc g++ libstdc++6 bison flex \
  perlmagick imagemagick libtiff4-dev \
  subversion

Install perl module prerequisites:

 perl -MCPAN -e 'install IO::Compress::Zlib'
 perl -MCPAN -e 'install IO::Compress::Gzip'
 perl -MCPAN -e 'install IO::Uncompress::Gunzip'
 perl -MCPAN -e 'install MogileFS::Client'
 perl -MCPAN -e 'install Digest::MD5'

Create /usr/local/polony-tools so that www-data can install stuff:

 sudo mkdir /usr/local/polony-tools
 sudo chown www-data /usr/local/polony-tools

Install the revision-installer:

 sudo svn export \
  http://dev.freelogy.org/svn/polony/polony-tools/trunk/installrevision \
  /usr/local/polony-tools/installrevision
